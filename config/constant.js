const memomryConvertingUnit = {
  UNIT: 1024,
};

const memoryUnit = {
  MB: 'MB',
};

const responseCodes = {
  SUCESS: 200,
  FAIL: 400,
};

const operatingSystem = {
  WINDOWS: 'win32',
};

const osBasePath = {
  WINDOWS: 'c:',
  OTHER: '/',
};

module.exports = {
  memomryConvertingUnit,
  memoryUnit,
  responseCodes,
  osBasePath,
  operatingSystem,
};
